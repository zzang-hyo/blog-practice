{
  "name": "blog-practice",
  "private": true,
  "version": "1.0.0",
  "description": "Markdown to static blog generator (no framework)",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
